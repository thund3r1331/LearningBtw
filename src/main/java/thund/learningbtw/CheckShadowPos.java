package thund.learningbtw;

import org.bukkit.Location;
import org.bukkit.Material;
import org.bukkit.entity.ArmorStand;

public class CheckShadowPos {
    //public void checkOnGround(ArmorStand armorStand) {
        //Location armorStandLocation = armorStand.getLocation();
        //Location blockUnder = armorStandLocation.subtract(0, 1, 0);
        //Material blockUnderArmorStand = blockUnder.getBlock().getType();

        //if (blockUnderArmorStand.equals(Material.AIR)){
            //Location newLocation = armorStandLocation.add(0,1,0);
            //armorStand.teleport(newLocation);
       // }
    //}
}
